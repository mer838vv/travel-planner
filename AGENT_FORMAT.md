# Формат плана от агента

Приложение — статика на GitHub Pages без бэкенда. Ключ от Claude API сюда
вшить нельзя (сайт публичный), поэтому агент работает снаружи: исследует
поездку своими скиллами и отдаёт результат в этом формате. Пользователь
вставляет его кнопкой «Вставить план от агента» на главном экране.

Импорт всегда создаёт **новую** поездку и ничего не перезаписывает.

## Схема

```json
{
  "trip": {
    "title": "Рим, октябрь",
    "startDate": "2026-10-03",
    "endDate": "2026-10-07",
    "destinationName": "Рим, Италия",
    "destinationLat": 41.9028,
    "destinationLon": 12.4964
  },
  "days": [
    {
      "date": "2026-10-03",
      "routeStart": {
        "type": "hotel",
        "name": "Hotel Artemide",
        "address": "Via Nazionale 22, Roma",
        "lat": 41.9008,
        "lon": 12.4926
      },
      "routeSummary": "Полный, но не перегруженный день в древнем центре.",
      "routeRationale": "Порядок сокращает возвраты, учитывает утренний билет в Колизей и оставляет Форум до жары, а вечер заканчивает рядом с отелем.",
      "breakSuggestion": "Обед 45 минут после Форума.",
      "walkingDistanceKm": 4.2,
      "walkingDurationMin": 58,
      "researchedAt": "2026-09-03",
      "routeSources": [{ "title": "Parco archeologico del Colosseo", "url": "https://colosseo.it/" }],
      "pois": [
        {
          "name": "Колизей",
          "description": "Амфитеатр 80 года, вмещал 50 тысяч зрителей. Билет общий с Форумом, действует два дня.",
          "lat": 41.8902,
          "lon": 12.4922,
          "visitTime": "10:00",
          "durationMin": 120,
          "cost": "18 €",
          "analysis": {
            "whyVisit": "Почему место существенно для понимания города, а не просто известно.",
            "highlights": ["Что увидеть первым", "Важная деталь, которую легко пропустить"],
            "practicalTip": "Где вход, сколько заложить на контроль и как избежать лишней очереди.",
            "bestTime": "Почему выбрано именно это время дня.",
            "booking": "Нужна ли бронь и где покупать официальный билет.",
            "sourceTitle": "Официальный сайт",
            "sourceUrl": "https://colosseo.it/",
            "checkedAt": "2026-09-03"
          }
        }
      ]
    }
  ],
  "tickets": [
    {
      "title": "SU2402 Москва — Рим",
      "category": "Авиабилет",
      "date": "2026-10-03",
      "note": "Багаж 23 кг включён",
      "flight": {
        "number": "SU2402",
        "pnr": "ABC123",
        "seats": ["20C", "20B"],
        "from": {
          "iata": "SVO", "name": "Шереметьево", "terminal": "C",
          "address": "Аэропорт Шереметьево", "tz": "Europe/Moscow"
        },
        "to": {
          "iata": "FCO", "name": "Рим, Фьюмичино", "terminal": "1",
          "tz": "Europe/Rome"
        },
        "departLocal": "2026-10-03T08:40",
        "arriveLocal": "2026-10-03T11:25",
        "checkinClosesLocal": "2026-10-03T07:55",
        "leaveAtLocal": "2026-10-03T05:40",
        "leaveNote": "дорога 50 мин + запас на пробки + контроль"
      }
    }
  ],
  "packing": [{ "category": "Документы", "name": "Паспорт" }],
  "budget": [
    { "title": "Отель, 4 ночи", "category": "Жильё", "amount": 420, "currency": "EUR" }
  ]
}
```


## Типы точек

Логистику (`flight`, `hotel`, `transfer`) приложение рисует плотной
карточкой с цветной рамкой и крупным заголовком: её пропуск стоит рейса
или ночёвки. Остальное оформляется мягче.

Тип угадывается по названию, если не задан, но угадывание ошибается:
«вилла» может быть и снятым жильём, и парком Боргезе. **Ставить `kind`
явно** — это одно поле, а разница в читаемости дня большая.

## Правила полей

| Поле | Обязательно | Замечания |
|---|---|---|
| `trip.title` | да | Любая строка |
| `trip.startDate` / `endDate` | да | Строго `ГГГГ-ММ-ДД`, конец не раньше начала |
| `trip.destinationLat` / `Lon` | нет | Без них не будет погоды и центрирования карты |
| `days[].date` | да | Дата вне диапазона поездки — день молча пропускается |
| `days[].routeStart` | **да для городского маршрута** | Отель (`hotel`), вокзал (`station`) или другая реальная точка (`custom`) с названием и координатами. Она становится исходной точкой Google Маршрута |
| `days[].routeRationale` | **да для серьёзного маршрута** | Не пересказ списка, а объяснение порядка с учётом расстояний, времени входа, очередей, жары и брони |
| `days[].routeSummary` | нет | Коротко: темп и смысл дня |
| `days[].walkingDistanceKm` / `walkingDurationMin` | нет | Проверенная оценка пешей части; не включать посещения и обед |
| `days[].researchedAt` / `routeSources` | **да для серьёзного маршрута** | Дата проверки и официальные источники. Агрегатор допустим только когда первичного источника нет |
| `pois[].name` | да | Без названия точка пропускается |
| `pois[].lat` / `lon` | **да** | **Без координат точка пропускается** — она не встанет на карту |
| `pois[].description` | **да для серьёзного маршрута** | Содержательное введение от 100 знаков; короткая подпись не считается анализом |
| `pois[].analysis` | **да для серьёзного маршрута** | `whyVisit`, минимум два `highlights`, `practicalTip`, `booking`, `bestTime`, официальный `sourceUrl` и дата `checkedAt` |
| `pois[].kind` | нет | Тип события: `flight`, `hotel`, `transfer`, `food`, `sight`, `nature`, `shop`, `other`. Задаёт иконку и цвет — рейс, заселение и прогулка не должны выглядеть одинаково. Без него тип угадывается по названию, но лучше ставить явно |
| `pois[].visitTime` | нет | Строка вида `10:00` |
| `pois[].durationMin` | нет | Число минут |
| `pois[].cost` | нет | Строка, можно с валютой: `18 €` |
| `tickets[].category` | нет | Только из списка ниже, иначе станет `Другое` |
| `tickets[].note` | нет | Номер брони, терминал, адрес заселения |
| `budget[].amount` | да | Число; строка вида `"55.5"` тоже принимается |
| `budget[].currency` | нет | По умолчанию `EUR`, приводится к верхнему регистру |

Категории билетов: `Авиабилет`, `Отель`, `Поезд/автобус`, `Музей/экскурсия`,
`Страховка`, `Виза`, `Другое`.

## Блок `flight` внутри билета

Необязателен: у страховки или музея его нет, и билет остаётся обычным
билетом. Если он есть — приложение показывает карточку ближайшего рейса с
терминалом, обратными отсчётами и проверкой стыковки.

| Поле | Обязательно | Замечания |
|---|---|---|
| `from` / `to` | да | Без обоих блок игнорируется целиком |
| `from.iata` | да | Код аэропорта, `SVO`, `FCO` |
| `from.terminal` | нет | **Самое ценное поле.** Показывается крупно: в городах с несколькими аэропортами и терминалами именно оно решает, куда ехать |
| `from.address` | нет | Идёт в кнопку «скопировать для такси»; без него берётся `name` |
| `from.tz` | **фактически да** | IANA-имя (`Europe/Rome`). **Без пояса не будет ни одного отсчёта и длительности перелёта** |
| `departLocal` / `arriveLocal` | да | **Местное** время `ГГГГ-ММ-ДДTЧЧ:ММ`, как в билете. Пересчёт в UTC приложение делает само по `tz` |
| `checkinClosesLocal` | нет | Момент закрытия онлайн-регистрации |
| `leaveAtLocal` | нет | Когда выезжать. Считает агент: дорога + пробки + контроль + запас |
| `leaveNote` | нет | Из чего сложилось время выезда — человек должен видеть логику, а не голое число |
| `pnr` | нет | Номер брони. **Разные `pnr` у соседних рейсов = раздельные билеты**, приложение отдельно предупредит о риске стыковки |
| `seats` | нет | Массив строк |

Стыковки приложение считает само по соседним рейсам: длительность, смена
аэропорта, раздельные билеты. Порог — от трёх часов для раздельных броней,
от полутора для единого билета; ниже часа (для единого билета — сорока пяти
минут) критично. Смена аэропорта не критична сама по себе, а добавляет два
часа к обоим порогам: это надбавка на дорогу и повторную регистрацию.

## Блок `transit` внутри `flight`

Проверенный ответ на вопрос, пройдёт ли человек пересадку по чистой зоне без
въезда в страну. Кладётся в `flight` того рейса, который **прилетает** в
аэропорт пересадки, — его карточка и висит перед глазами в полёте.

Собирается скиллом `transit-check`. **Блока нет — приложение прямо пишет
«не проверено».** Это намеренно: раньше оно само сочиняло ответ и однажды
уверенно заявило, что по раздельным билетам транзитную зону в Стамбуле не
пройти. Пройти удалось. Угадывать здесь нельзя ни в какую сторону.

```json
"transit": {
  "airport": "IST",
  "verdict": "ok",
  "citizenship": "RU",
  "steps": [
    "После прилёта идти по указателям Transfer, не в Passport Control",
    "Отсканировать посадочный на Москву на турникете трансфера",
    "Пройти повторный досмотр"
  ],
  "boardingPass": "Получить онлайн-регистрацией до вылета из Рима, сохранить офлайн",
  "baggage": "Только ручная кладь — получать ничего не нужно",
  "visaNote": "Гражданам РФ транзитная виза для чистой зоны не нужна",
  "warnings": ["Билеты разными бронями: при опоздании первого рейса пересаживать не обязаны"],
  "checkedOn": "2026-09-01",
  "sources": [{ "title": "IST — Transfer passengers", "url": "https://www.istairport.com/..." }]
}
```

| Поле | Обязательно | Замечания |
|---|---|---|
| `verdict` | да | `ok` — транзит без въезда подтверждён; `needs-entry` — подтверждено, что нужен въезд; `unknown` — проверить не удалось |
| `checkedOn` | **фактически да** | Дата проверки `ГГГГ-ММ-ДД`. Без неё и без `sources` вердикт понижается до `unknown`: правила меняются, и утверждение без даты ничем не отличается от догадки |
| `sources` | **фактически да** | Массив `{title, url}`. Запись без `url` отбрасывается |
| `citizenship` | нет | Под какой паспорт дан ответ. Транзитные правила пишутся под паспорт, а не под маршрут |
| `steps` | нет | Что делать по шагам после прилёта. Показывается нумерованным списком |
| `boardingPass` / `baggage` / `visaNote` | нет | Три отдельные строки — их путают между собой, поэтому и разведены |
| `warnings` | нет | До шести строк |
| `minConnectionMin` | нет | MCT аэропорта, минут |

Что приложение говорит **само**, без этого блока: раздельные билеты означают,
что багаж не проследует, посадочный надо получить заранее и при опоздании
пересаживать не обязаны. Это следствия раздельных броней, и они верны всегда.
А вот проходимость транзитной зоны из числа броней не следует.

## Блок `transfer` внутри `flight`

Как доехать до аэропорта: варианты с ценами и инструкцией. Приложение само
находит самый дешёвый, помечает его и считает, **насколько дороже каждый
остальной** — и в деньгах, и в процентах. Собирается скиллом `transfer-floor`.

```json
"transfer": {
  "from": "Hilton Garden Inn Rome Colosseum, Via Emanuele Filiberto 173",
  "durationMin": 40,
  "warnings": ["В обычном такси сразу сказать «tariffa fissa Fiumicino»"],
  "options": [
    {
      "name": "Uber",
      "price": 55, "currency": "EUR", "priceRub": 5531, "unit": "за машину",
      "note": "Цена зафиксирована в приложении до заказа",
      "howTo": "Заказать на 04:10 из приложения",
      "url": "https://m.uber.com/"
    },
    {
      "name": "Такси по фиксированному тарифу",
      "price": 58.5, "currency": "EUR", "unit": "за машину",
      "howTo": "Попросить ресепшн заказать — язык не нужен",
      "phone": "+39 06 3570",
      "phrase": "Buonasera! Vorrei prenotare un taxi...",
      "phraseTranslation": "Добрый вечер! Хочу заказать такси..."
    },
    { "name": "Bolt", "unavailable": true, "note": "В Риме не работает" }
  ]
}
```

| Поле | Замечания |
|---|---|
| `options[].name` | Обязательно, иначе вариант отбрасывается |
| `price` + `currency` | **Все варианты в одной валюте** — иначе разница не показывается вовсе, чтобы не складывать евро с лирами |
| `unit` | «за машину» или «с человека». **Обязательно указывать:** такси стоит за поездку, поезд с человека, и без пометки сравнение врёт |
| `priceRub` | Рубли по курсу ЦБ — правило 9 профиля |
| `howTo` | Что конкретно сделать. Выводится жирным — это главное действие |
| `phrase` / `phraseTranslation` | Фраза на местном языке с переводом, копируется кнопкой |
| `phone` | Кликается и звонит с телефона |
| `unavailable` | Сервис не работает в этом городе. Показывается серым — полезно знать, что вариант проверен и отпал |

Цену ride-hailing брать **из живого приложения**, а не из статей: статьи
«такси против Uber» обычно пишут продавцы трансферов и завышают конкурента.

Файлы (сканы билетов) агент приложить не может — их пользователь добавляет
сам во вкладке «Билеты». Номер брони при этом стоит положить в `note`.

## Что проверяется тестами

`npm test` — `test/agentImport.test.js`. Покрыто: раскладка по таблицам,
снятие обёртки ` ```json `, отказ на мусоре с понятным сообщением,
пропуск битых записей без потери остального плана, неприкосновенность
уже существующих поездок.
